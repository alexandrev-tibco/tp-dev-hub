apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: cp-admin-webserver
  description: Serves the TIBCO Platform Console (Admin) Web UI for managing subscriptions and configurations.
spec:
  owner: team-platform
  type: service
  lifecycle: production
  subcomponentOf: control-plane
